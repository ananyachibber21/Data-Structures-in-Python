# Creating an array

arr = []
size = int(input("Enter the size of the array: "))
for i in range(0,size):
    val = int(input("Enter the value: "))
    arr.append(val)

# Sorting an array

sorting = sorted(arr)
print(sorting)

# Other Sorting Techniques - Bubble Sort, Selection Sort, Insertion Sort, Quick Sort, ...